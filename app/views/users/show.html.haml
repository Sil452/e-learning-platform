= render 'users/user', user: @user

.card-header
  .text-center
    %h5 my courses
.card-footer
  .row
    - @user.courses.each do |course|
      .col-xs-12.col-md-3
        = link_to  course.title, course_path(course)